# Code Clash 2: Battle of Algorithms

1) 🚀 Na základě přiloženého kódu dokončete algoritmus Bubblesort a vizualizaci.
    1) Upravte BubbleSort tak aby už neřadil již seřazené prvky na konci poli.
    2) Přidejte do BubbleSortu optimalizaci na ukončení pokud po průchodu celého pole nedošlo k prohození prvků (pole již tedy bylo seřazeno).
    3) Přidejte do BubbleSortu property IsSorted, která bude `true` pokud je už pole seřazeno, jinak bude `false`.
    4) Do komponenty Numbers přidejte parametr `SecondPosition` který bude zobrazovat index s jinou barvou podtržení.
    5) Do komponenty Numbers přidejte parametry `HighlightFrom` a `HighlightTo`, které budou označovat část pole jinou barvou písma.
    6) Do komponenty Number přidejte parametr `IsSorted`, kdy pokud bude true, tak zobrazí text jinou barvou a přidá na konec čísel znak signalizující dokončení seřazení.

2) 🚀 Podle vzoru BubbleSortu doplňte algoritmy **InsertionSort**, **SelectionSort** a **QuickSort**.
